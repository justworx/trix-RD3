{

	#
	# JConfig Wrapper configuration
	#  - under construction
	#
	
	commands : {
		"open" : {
			"cmd" : "open",
			"help" : "Open a JSON (or ast-parsable) conig file.",
		},
		
		"save" : {
			"cmd" : "save",
			"help" : "Open a JSON (or ast-parsable) config file."
		},
		
		"set" : {
			"cmd" : "set",
			"help" : "Open a JSON (or ast-parsable) config file."
		}
	}
}